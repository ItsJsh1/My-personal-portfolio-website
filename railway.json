{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "ls -la public/build/ && cat public/build/manifest.json && php artisan optimize:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}